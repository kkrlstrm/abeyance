# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
venv/

# local loop state (regenerable; never a source of truth worth committing)
.abeyance-state/
*.abeyance.json

# env / secrets
.env
token.json
credentials.json

# OS
.DS_Store

# smoke-run scratch: which case a local rehearsal is pointed at. Per-machine, not a fact.
examples/.smoke-case-id
examples/.recovery-case-id

# build artifacts
dist/
build/
*.egg-info/
